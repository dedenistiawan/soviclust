# R / RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata

# R package build artefacts
*.tar.gz
*.zip
src/*.o
src/*.so
src/*.dll
inst/doc/
doc/

# devtools / roxygen
# man/ — JANGAN diabaikan: diperlukan agar package bisa install dari GitHub
Meta/

# Shinyapps deployment
rsconnect/

# pkgdown website build (dikelola GitHub Actions, tidak perlu di-commit)
docs/
pkgdown/
/doc/
/Meta/

# Shapefile mentah (terlalu besar untuk GitHub) — pakai .rds yang sudah dikompres
inst/app/map/*.shp
inst/app/map/*.dbf
inst/app/map/*.prj
inst/app/map/*.qpj
inst/app/map/*.shx
inst/app/map/*.cpg

# Local validation patch backups / temporary patch installers
validation/patch_backups/
validation/patch_v3/
validation/patch_v3_*_fix/
validation/patch_v3_test_isolation/

# Reproducible binary validation outputs
validation_results/**/*.rds
